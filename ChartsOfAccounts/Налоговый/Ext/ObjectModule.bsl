Перем ЗапрещенныеСчета Экспорт;

Процедура ПередЗаписью(Отказ)

	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если Забалансовый = 0 Тогда
		Забалансовый = 1;
	КонецЕсли;

	Порядок = ПолучитьПорядокКода();
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	Если НЕ ЗначениеЗаполнено(КодБыстрогоВыбора) Тогда
		КодБыстрогоВыбора = СокрЛП(СтрЗаменить(Код, ".", ""));
	КонецЕсли;
КонецПроцедуры

//ЗапрещенныеСчета = Новый СписокЗначений();
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.ОборудованиеКУстановкеИСтроительныеМатериалы);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.ПриобретениеОбъектовОсновныхСредств);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.Полуфабрикаты);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.Товары);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.ГотоваяПродукция);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.Материалы);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.ТоварыОтгруженные);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.ТоварыПринятыеНаКомиссию);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.РасчетыСПерсоналомПоОплатеТруда);
//ЗапрещенныеСчета.Добавить(ПланыСчетов.Налоговый.РасходыНаОплатуТрудаБудущихПериодов);
